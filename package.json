{
  "scripts": {
    "install:all": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "generate:middle-words": "node scripts/generate-middle-school-words.mjs",
    "emulate": "npm run build && firebase emulators:start",
    "deploy": "npm run build && firebase deploy --project wordday-fire",
    "deploy:hosting": "npm run build && firebase deploy --only hosting --project wordday-fire"
  },
  "devDependencies": {
    "firebase-tools": "^15.22.4",
    "playwright": "^1.60.0"
  }
}
