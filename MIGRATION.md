# Render 버전과 달라진 점

- 원본은 `C:\Users\user\wordday(render)`에 보존되어 있습니다.
- 새 버전은 Firebase 무료 Spark 요금제만 사용합니다.
- 교사는 자유 아이디, 비밀번호, 보안 질문 답변으로 가입합니다.
- 학생은 기존처럼 학급 코드, 학번, 비밀번호로 로그인합니다.
- Firebase Authentication, Firestore, 보안 규칙만 사용합니다.
- 기존 PostgreSQL 데이터는 자동 이전되지 않습니다.
